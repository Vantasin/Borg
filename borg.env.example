# Borg automation environment (do not commit the real borg.env)
# Copy to /usr/local/sbin/borg/borg.env, set values, and chmod 600 (owner root:root).

# Required: Borg repository passphrase (keep secret)
BORG_PASSPHRASE=change-me

# Paths: adjust to your ZFS layout and desired repo location
BORG_REPO=/tank/Secure/Borg/backup-repo   # Target Borg repository path (must exist or be creatable)
SOURCE_PATH=/tank/Secure/backup           # Source directory to back up
ZFS_DATASET=tank/Secure/backup            # ZFS dataset for SOURCE_PATH; used for snapshotting/availability checks
REPO_DATASET=tank/Secure/Borg             # Optional: dataset hosting BORG_REPO; used for availability checks
LOG_DIR=/var/log/borg                     # Log directory used by backup/check scripts

# Notifications: msmtp envelope addresses
MAIL_TO=alerts@example.com                # Recipient for backup/check notifications
MAIL_FROM=borg@localhost                  # Sender; should match an msmtp account (default account is used)
MAIL_ON_SUCCESS=true                      # true/false/1/0 to send or suppress success emails
MAIL_ON_FAILURE=true                      # true/false/1/0 to send or suppress failure emails
